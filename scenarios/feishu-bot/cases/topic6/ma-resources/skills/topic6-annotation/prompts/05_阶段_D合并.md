# prompt/05 · 阶段 D：七路合并

> 迁移首轮：原 `agent/skill/phases/D_merge.md`（跳转页）全文 + `agent/CLAUDE.md`「HC2」小节。
> 内容未改写。
>
> 合并脚本参数与健康度摘要维护在 `skill/annotation/SKILL.md`「六、执行流程」Step 8。
> 宽表 28 列的字段定义见 `ks/_字段速查.md`（权威在 annotation SKILL 的输出契约）。

---

# Phase D · 合并（C0+R1~R5+C2+C3 七路 LEFT JOIN）

> 状态：可用
>
> 本文件不重复详细步骤，Phase D 的完整执行说明（脚本参数、健康度摘要、失败处理）
> 统一维护在：
> **`skill/annotation/SKILL.md`「六、执行流程」Step 8**
>
> 本文件只做跨阶段总览，供主调度文件（`prompt/01_pipeline总览.md`）
> 在"执行流程"表格中引用。

---

## 前置条件

- [ ] C0+R1~R5+C2+C3 七路标注均已完成后处理（见 `skill/annotation/SKILL.md` Step 3/7）

## 执行步骤（摘要，详见 skill/annotation/SKILL.md Step 8）

```bash
python skill/annotation/scripts/merge_annotations.py \
  --project-dir "Projects/{PROJECT_DIR}" \
  --mode test \
  --run-id {N}
```

产出：`05_合并/wide_table_{mode}_r{N}.xlsx`（七路 LEFT JOIN 到基础表）+
`05_合并/health_summary_{mode}_r{N}.md`（健康度摘要）。

## 输出验证

- [ ] 宽表行数 = 基础表行数，`row_id` 唯一
- [ ] C0 解析失败/缺失占比 ≤ 5%（超过需先走补标注闭环）
- [ ] R1~R5/C2 覆盖率 = 可用子集行数（不是全量行数，LEFT JOIN 后其余行天然为空，不是 bug）

## 成本记录

`merge_annotations.py` 为纯脚本，无 LLM token 费用，**不调用 cost_tracker.py**。

---

禁止：有效率 >= 90% 时自动通过 / 合并 HC1 与 HC2 为一次确认

### HC2 — 全量标注验收

触发时机：D-全量合并完成后（current_phase=hc2_wait）

**Step 1：spawn Validator 质检**（先于展示表格执行）

用 Agent tool 以 `model: "opus"` spawn Validator，prompt 中只给：
- 待查文件路径：`05_合并/wide_table_full_r{N}.xlsx`（全量合并宽表）、`05_合并/health_summary_full_r{N}.md`（健康度摘要）
- Producer 已算好的全量验收指标表 + 平台分布表（本节下方表格，数据算好后再传入，不重复计算）
- HC1 阶段若有遗留问题，一并传入供 Validator 核对"是否基本解决"
- 判断依据：明确指向 `Agent.Validator/KS/KS_Checklists/Topic6_热点报告检查清单.md` §HC2，仅用该节规则判断，不越界参考 §HC1/§HC3

不在 spawn prompt 中替 Validator 下结论，不删减上述判断依据。

等待 spawn 返回。Validator 执行期间会依 `Agent.Validator/.claude/rules/output_formats.md` §三·bis 自行把结论通过 IM 推送到同一 chat_id（即本会话），故用户在看到下方表格前会先看到一条 Validator 结论消息。

**Step 2：展示以下内容，等待用户回复**（格式与判定标准不变）：

```
HC2 全量标注验收

| 指标                | 值    |
|---------------------|-------|
| 总行数              | {N}   |
| C0 空值率           | {x}%  |
| C0 是否营销可用占比 | {x}%  |
| R1~R5 子集行数      | {N}   |
| C2 聚类覆盖率       | {x}%  |
| C3 节点覆盖率       | {x}%  |
| 最大单 cluster 规模 | {N} 条|

平台分布：
| 平台 | 行数 | 占比 |
|------|------|------|
| 微博 |      |      |
| 抖音 |      |      |
| B站  |      |      |
| 知乎 |      |      |

合并宽表：[wide_table_full_r*.xlsx](<file:///Projects/{PROJECT_DIR}/05_合并/>)

通过请回复「HC2通过」；不通过请回复「HC2不通过 {原因}」。
```
