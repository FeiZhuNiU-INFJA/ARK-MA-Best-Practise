# prompt/04 · 阶段 C：七路标注

> 迁移首轮：原 `agent/skill/phases/C_annotation.md`（跳转页）全文 + `agent/CLAUDE.md`「HC1」小节。
> 内容未改写。
>
> **阶段内部步骤不在本文件** —— Step 0~11、DataHub 提交/轮询命令、5% 熔断阈值、3 轮补标注上限、
> 系统性故障熔断，全部维护在 `skill/annotation/SKILL.md`「六、执行流程」。本文件只管跨阶段编排与关卡。
>
> 判定口径：`ks/02_营销可用判定.md`（C0 总闸门）、`ks/03_五维判定.md`（R1~R5）、`ks/04_节点判定.md`（C3）。

---

# Phase C · 标注（C0基础事实层 + R1~R5版块路由层 + C2事件合并 + C3节点标注）

> 版本：v2.0 | 更新日期：2026-09-17
> 状态：可用
>
> 本文件不重复详细步骤，Phase C 的完整执行说明（Prompt路径、DataHub提交/轮询命令、
> 筛选熔断阈值、补标注闭环、七路合并）统一维护在：
> **`skill/annotation/SKILL.md`**
>
> 本文件只做跨阶段总览，供主调度文件（`prompt/01_pipeline总览.md`）
> 在"执行流程"表格中引用。

---

## 前置条件

- [ ] Phase B 已完成，`02_标准化/hot_topics_normalized.xlsx` 存在且无 NaN
- [ ] 小样本模式：`03_抽样/sample_500.xlsx` 已生成（`scripts/sample_500.py`）

## 执行步骤（摘要，详见 skill/annotation/SKILL.md）

1. 第一批并发：C0（基础事实层）+ C3（节点标注）跑全量/全样本
2. 筛选（纯脚本零成本）：`c0_merge_phase1.py` → `c0_filter_usable.py`，按"是否营销可用"筛出子集
3. 第二批并发：R1~R5（版块路由层）+ C2（事件合并），只处理筛选后子集
4. 七路合并：`merge_annotations.py`（C0+R1~R5+C2+C3 LEFT JOIN 到基础表）

## 输出验证

- [ ] 合并宽表行数 = 输入基础表行数，`row_id` 唯一
- [ ] C0 解析失败/缺失占比 ≤ 5%（超过需先走补标注闭环）
- [ ] R1~R5/C2 覆盖率 = 可用子集行数（不是全量行数，LEFT JOIN 后其余行天然为空，不是 bug）

## 成本记录

每个 DataHub 任务完成后由 `skill/annotation/scripts/datahub_poll.py` 自动调用
`tool/cost-tracker/cost_tracker.py` 记录成本，无需手动执行。

---

3 个 HC 均不得自动通过，必须等用户明确回复后才能继续。

### HC1 — 小样本验收（500 条）

触发时机：D-小样本合并完成后（current_phase=hc1_wait）

**Step 1：spawn Validator 质检**（先于展示表格执行）

用 Agent tool 以 `model: "opus"` spawn Validator，prompt 中只给：
- 待查文件路径：`04_合并/phase1_merged_test_r{N}.xlsx`、`04_标注/_可用子集/usable_subset_test_r{N}.xlsx`
- Producer 已算好的 8 行质量表（本节下方表格，数据算好后再传入，不重复计算）
- 判断依据：明确指向 `Agent.Validator/KS/KS_Checklists/Topic6_热点报告检查清单.md` §HC1，仅用该节规则判断，不越界参考 §HC2/§HC3

不在 spawn prompt 中替 Validator 下结论（不得暗示"这批应该没问题"等），不删减上述判断依据。

等待 spawn 返回。Validator 执行期间会依 `Agent.Validator/.claude/rules/output_formats.md` §三·bis 自行把结论通过 IM 推送到同一 chat_id（即本会话），故用户在看到下方表格前会先看到一条 Validator 结论消息。

**Step 2：展示以下内容，等待用户回复**（格式与判定标准不变）：

```
HC1 小样本验收（500 条）

| 任务          | 有效条数   | 有效率 | 主要异常            |
|---------------|------------|--------|---------------------|
| C0 基础事实   | {n} / 500  | {x}%   | {无 / 描述}         |
| R1 平台借势   | {n} / {N}  | {x}%   | {无 / 描述}         |
| R2 商业合作   | {n} / {N}  | {x}%   | {无 / 描述}         |
| R3 风险预警   | {n} / {N}  | {x}%   | {无 / 描述}         |
| R4 创意借鉴   | {n} / {N}  | {x}%   | {无 / 描述}         |
| R5 消费者行为 | {n} / {N}  | {x}%   | {无 / 描述}         |
| C2 事件合并   | {n} / {N}  | {x}%   | {无 / 描述}         |
| C3 节点标注   | {n} / 500  | {x}%   | {无 / 描述}         |

说明：R1~R5/C2 的分母 {N} 是 C0 筛选出的"营销可用"子集行数，不是 500；
C0 的"是否营销可用"占比、"不可用原因"分布也需要一并展示，供人工判断筛选是否合理。

抽查文件：[phase1_merged_test_r{N}.xlsx](<file:///Projects/{PROJECT_DIR}/04_合并/>)
        [usable_subset_test_r{N}.xlsx](<file:///Projects/{PROJECT_DIR}/04_标注/_可用子集/>)

通过请回复「HC1通过」；不通过请回复「HC1不通过 {原因}」。
```

禁止：有效率 >= 90% 时自动通过 / 合并 HC1 与 HC2 为一次确认
