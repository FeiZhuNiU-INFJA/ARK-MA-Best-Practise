# prompt/03 · 阶段：分层抽样（500 条）

> **本文件是迁移时新写的，没有原始来源。**
>
> 迁移前，这一步**不在任何一张流程表里**：`agent/CLAUDE.md`「工作流程」表和 `agent/skill/SKILL.md`
> 「执行流程」表都直接从「A+B 取数标准化」跳到「C-小样本」，只有 `phases/C_annotation.md` 的前置条件里
> 和 `run_config.yaml` 的 `sample` 状态块提到过它。
>
> 后果：Agent 严格照流程表执行会跳过抽样，然后在提交 C0/C3 时报「`sample_500.xlsx` 不存在」——
> 这是 100% 会触发的阻塞，不是偶发。本文件即为补漏。

---

## 定位

| 项 | 内容 |
|---|---|
| 前序 | 阶段 A 完成（`02_标准化/hot_topics_normalized.xlsx` 存在） |
| 后续 | 阶段 C 第一批（C0 + C3 并发），**仅 test 模式需要本阶段** |
| 成本 | 纯脚本，无 LLM 调用，**不调用 cost_tracker** |
| run_config 状态块 | `status.sample` |

**full 模式不经过本阶段** —— 全量轮直接用 `02_标准化/hot_topics_normalized.xlsx` 作为 C0/C3 的输入。

---

## 前置条件

- [ ] `status.ab.completed == true`
- [ ] `02_标准化/hot_topics_normalized.xlsx` 存在且无 NaN

---

## 执行

```bash
python skill/fetch-normalize/sample_500.py \
    --project-dir "Projects/{PROJECT_DIR}"
```

`--project-dir` 模式会自动推断输入输出路径（`02_标准化/hot_topics_normalized.xlsx` →
`03_抽样/sample_500.xlsx`）。也可用 `--input` / `--output` 显式指定，二者互斥。

可选参数：`--size`（默认 500）、`--seed`（默认 42）。

---

## 抽样口径

| 项 | 规则 |
|---|---|
| 分层依据 | `platform` 列（微博/抖音/B站/知乎） |
| 配额分配 | 按各平台行数占比，**最大余数法**（Hamilton）确保总数恰好 = 500 |
| 平台行数不足配额时 | 取该平台全量，余量追加给行数最大的平台 |
| 随机种子 | 固定 `seed=42`，**结果可复现** |

**为什么要分层而不是简单随机**：四平台行数差异大（微博约占 54%），简单随机会让小平台在 500 条样本里只剩个位数，HC1 无法判断该平台的标注质量是否达标。

---

## 输出

| 文件 | 说明 |
|---|---|
| `03_抽样/sample_500.xlsx` | 500 行（总量不足时为全量），保留输入的全部列 |

---

## 一个需要留意的提示

当数据总量 ≤ 抽样上限时，脚本会输出：

> 当前 test 模式 = full 模式（无抽样偏差），可跳过 test 验证步骤，直接用 full 模式运行后续全流程。

这种情况下 HC1 与 HC2 验收的是同一批数据，**但两个 HC 仍然都不能跳过**（见根 `CLAUDE.md` 的 HC 规范）——
它们检查的对象不同：HC1 看标注质量，HC2 看全量分布与覆盖率。

---

## 完成后更新 run_config.yaml

```yaml
status:
  sample:
    completed:    true
    completed_at: "{ISO时间}"
    sample_file:  "03_抽样/sample_500.xlsx"
    sample_rows:  {实际行数}
  current_phase:  "c0_sample"
```

→ 进入 `prompt/04_阶段_C标注.md`
