"""数字员工可视化管理系统（管理后端 + 静态前端）。

对齐 case 目录的扁平 import 约定：``server`` / ``run_admin`` 以顶层模块方式被引用
（测试与入口把 ``cases/digital-employee`` 与本目录加入 ``sys.path``）。本文件同时让
``admin`` 作为包名可被 ``import admin.server`` 引用，两种方式并存。
"""
